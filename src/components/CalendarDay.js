import {useState, useEffect} from 'react'

function CalendarDay({box, today, firstOfMonth, daysInMonth}) {

    const start = () => {
        
    }

    return (
        <div >{box}</div>
)}

export default CalendarDay;